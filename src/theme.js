import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#556cd6',
      light: 'skyblue',
    },
    secondary: {
      main: '#19857b',
    },
    otherColor: {
      main: 'orange',
    }
  }
});